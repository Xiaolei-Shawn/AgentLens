{
  "name": "@al/mcp-server",
  "version": "0.1.0",
  "description": "AL MCP server — event ingestion, session assembly, flush, list_sessions, folder watcher",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "al-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@al/schema": "file:../schema",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
